name: Svelte

# Not supported
# pre_window: command
# startup_window: Neovim
# startup_pane: Neovim

windows:
  - name:  Neovim
    panes:
      - nvim
  - name: Server
    layout: even-horizontal
    panes:
      - pnpm run dev
      - pnpm run test
